export function resolveRendererUrl(publicUrl, filename) {
    if (publicUrl === './')
        return filename;
    if (publicUrl.endsWith('/'))
        return publicUrl + filename;
    return `${publicUrl}/${filename}`;
}
