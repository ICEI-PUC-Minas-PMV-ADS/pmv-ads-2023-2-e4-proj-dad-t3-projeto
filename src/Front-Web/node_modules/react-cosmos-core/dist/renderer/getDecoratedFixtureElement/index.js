import React from 'react';
import { FixtureCapture } from '../FixtureCapture/index.js';
import { FixtureElement } from './FixtureElement.js';
export function getDecoratedFixtureElement(fixture, decorators, decoratorProps) {
    const fixtureElement = (React.createElement(FixtureCapture, { decoratorId: "root" }, getFixtureElement(fixture)));
    return [...decorators]
        .reverse()
        .reduce((prevElement, Decorator) => (React.createElement(Decorator, { ...decoratorProps }, prevElement)), fixtureElement);
}
function getFixtureElement(fixture) {
    return isFunctionFixture(fixture) ? (React.createElement(FixtureElement, { Component: fixture })) : (fixture);
}
function isFunctionFixture(fixture) {
    return typeof fixture === 'function';
}
