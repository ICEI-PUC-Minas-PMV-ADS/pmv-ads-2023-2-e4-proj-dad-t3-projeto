import { RendererConnect } from './rendererConnectTypes.js';
import { ReactFixtureModule } from './userModuleTypes.js';
type Props = {
    rendererId: string;
    rendererConnect: RendererConnect;
    fixturePath: string;
    fixtureModule: ReactFixtureModule;
};
export declare function LazyFixtureListItemUpdate({ rendererId, rendererConnect, fixturePath, fixtureModule, }: Props): null;
export {};
