import { ReactElement } from 'react';
import { FixtureId } from '../fixture/types.js';
import { RendererConnect } from './rendererConnectTypes.js';
import { ReactDecorator, UserModuleWrappers } from './userModuleTypes.js';
type Props = {
    rendererId: string;
    rendererConnect: RendererConnect;
    moduleWrappers: UserModuleWrappers;
    systemDecorators: ReactDecorator[];
    initialFixtureId?: FixtureId;
    selectedFixtureId?: null | FixtureId;
    renderMessage?: (msg: string) => ReactElement;
    renderNoFixtureSelected?: boolean;
    onErrorReset?: () => unknown;
};
export declare function FixtureConnect({ rendererId, rendererConnect, moduleWrappers, systemDecorators, initialFixtureId, selectedFixtureId, onErrorReset, renderMessage, renderNoFixtureSelected, }: Props): JSX.Element | null;
export {};
