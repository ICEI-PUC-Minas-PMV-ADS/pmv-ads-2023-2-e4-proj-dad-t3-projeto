import { ReactElement } from 'react';
import { FixtureId } from '../fixture/types.js';
import { FixtureState, SetFixtureState } from '../fixtureState/types.js';
import { ByPath, ReactDecorator, ReactDecoratorWrapper, ReactFixtureWrapper } from './userModuleTypes.js';
type Props = {
    fixtureWrapper: ReactFixtureWrapper;
    decorators: ByPath<ReactDecoratorWrapper>;
    systemDecorators: ReactDecorator[];
    fixtureId: FixtureId;
    fixtureState: FixtureState;
    setFixtureState: SetFixtureState;
    renderMessage: (msg: string) => ReactElement;
    renderKey: number;
    onErrorReset?: () => unknown;
};
export declare function FixtureLoader({ fixtureWrapper, decorators, systemDecorators, fixtureId, fixtureState, setFixtureState, renderMessage, renderKey, onErrorReset, }: Props): JSX.Element;
export {};
